ГСЧ = Новый ГенераторСлучайныхЧисел();
МассивА = Новый Массив(5);
МассивБ = Новый Массив(5);

СуммаБольшеПяти = 0;
КоличествоМеньшеПяти = 0;

Для Шаг = 0 По 4 Цикл
	МассивА[Шаг] = ГСЧ.СлучайноеЧисло(0, 10);
	МассивБ[Шаг] = ГСЧ.СлучайноеЧисло(0, 10);
	Сообщить("Массив А[" + Шаг + "]: " + МассивА[Шаг]);
	Сообщить("Массив Б[" + Шаг + "]: " + МассивБ[Шаг]);
КонецЦикла;

Для каждого ЭлементМассива из МассивА Цикл
	Если (ЭлементМассива > 5) Тогда
		СуммаБольшеПяти = СуммаБольшеПяти + ЭлементМассива
	Иначе
		КоличествоМеньшеПяти = КоличествоМеньшеПяти + 1;
	КонецЕсли;
КонецЦикла;

Для каждого ЭлементМассива из МассивБ Цикл
	Если (ЭлементМассива > 5) Тогда
		СуммаБольшеПяти = СуммаБольшеПяти + ЭлементМассива
	Иначе
		КоличествоМеньшеПяти = КоличествоМеньшеПяти + 1;
	КонецЕсли;
КонецЦикла;

Сообщить(СуммаБольшеПяти);
Сообщить(КоличествоМеньшеПяти);
